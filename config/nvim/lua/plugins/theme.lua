-- Omarchy regenerates this theme specification whenever the active theme changes.
-- Keep the loader in the repository while reading the generated file outside it.
return dofile(vim.fn.expand("~/.local/state/omarchy/current/theme/neovim.lua"))
